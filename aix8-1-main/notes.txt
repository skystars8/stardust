Use cargo run --bin aix8 (for development/debug mode).

Or, for a production-optimized build: cargo build --release, then ./target/release/aix8 <file> (replacing <file> with your target file).


aix8
├─ Cargo.toml
├─ src
│  ├─ lib.rs        ← crypto engine
│  └─ bin
│     └─ aix8.rs   ← CLI
└─ tests
   └─ integration_tests.rs